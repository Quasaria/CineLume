# Dependencies
node_modules/
.pnp
.pnp.js

# Lockfile : le workflow deploy utilise `npm install --legacy-peer-deps`
# (pas `npm ci`), donc on ne commit pas le lockfile pour eviter les conflits
# de peer deps. cf .github/workflows/deploy.yml
package-lock.json
yarn.lock
pnpm-lock.yaml

# Build output
dist/
dist-ssr/
build/

# Local env files (NE JAMAIS COMMIT)
.env
.env.local
.env.*.local

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
Thumbs.db

# Vite cache
.vite/

# Misc
*.tsbuildinfo
